[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: end-to-end tests requiring a live DB and API keys (deselect with -m "not integration")
filterwarnings =
    ignore::DeprecationWarning
