# ms-maf branch — Microsoft Agent Framework (MAF) research agent with pgvector RAG
# Install: pip install -r requirements.txt
# Docs:    https://aka.ms/agent-framework

# ── Web framework ──────────────────────────────────────────────
fastapi==0.120.4
uvicorn==0.38.0
starlette==0.49.1
python-multipart==0.0.20

# ── Microsoft Agent Framework ─────────────────────────────────
# agent-framework-core: Agent, @tool, AgentResponse, ResponseStream
# agent-framework-openai: OpenAIChatCompletionClient (Chat Completions API,
#   OpenAI-compatible → works with OpenRouter)
agent-framework-core==1.9.0
agent-framework-openai==1.8.2

# ── Embeddings (nomic-embed-text-v1.5 for vector search) ──────
sentence-transformers==5.1.2
torch==2.2.2
transformers==4.57.1
tokenizers==0.22.2
safetensors==0.6.2
huggingface-hub==0.36.0

# ── PostgreSQL + pgvector ──────────────────────────────────────
psycopg2-binary==2.9.11
pgvector==0.3.6

# ── Web tools ─────────────────────────────────────────────────
beautifulsoup4==4.14.2
requests==2.32.5
lxml==6.0.2
ddgs>=9.14.2

# ── NumPy/SciPy — torch 2.2.2 was built against numpy 1.x; scipy<1.14 matches ─
numpy<2
scipy<1.14

# ── OpenTelemetry (MAF native instrumentation → Phoenix) ──────
opentelemetry-exporter-otlp-proto-http

# ── Conversation summarization ────────────────────────────────
litellm

# ── Utilities ─────────────────────────────────────────────────
python-dotenv==1.2.1
pydantic==2.12.3
httpx==0.28.1
anyio==4.11.0

# ── LangChain-core (required internally by ragas) ─────────────
langchain-core==1.4.8
langsmith==0.4.39

# ── Testing (RAG evaluation) ──────────────────────────────────
ragas==0.4.3
deepeval==4.0.7
pytest==9.1.1
pytest-rerunfailures==16.3
