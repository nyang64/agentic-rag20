# crewai branch — CrewAI research agent with pgvector RAG
# Install: pip install -r requirements.txt

# ── Web framework ──────────────────────────────────────────────
fastapi==0.120.4
uvicorn==0.38.0
starlette==0.49.1
python-multipart==0.0.20

# ── CrewAI agent stack ─────────────────────────────────────────
# crewai: Agent, Task, Crew, Process, LLM, BaseTool
# litellm: LLM gateway used by CrewAI (OpenRouter via "openrouter/" prefix)
crewai==1.9.3
litellm==1.90.0

# ── OpenAI-compatible client (OpenRouter via litellm) ─────────
openai==2.44.0

# ── LangChain-core (used by RAGAS evaluation framework internally) ─
langchain-core==1.4.8
langsmith==0.4.39

# ── Embeddings (nomic-embed-text-v1.5 for vector search) ──────
sentence-transformers==5.1.2
torch==2.2.2
transformers==4.57.1
tokenizers==0.20.3
safetensors==0.6.2
huggingface-hub==0.36.0

# ── PostgreSQL + pgvector ──────────────────────────────────────
psycopg2-binary==2.9.11
pgvector==0.3.6

# ── Web scraping (content ingestion) ──────────────────────────
Scrapy==2.13.3
beautifulsoup4==4.14.2
requests==2.32.5
lxml==6.0.2
playwright==1.55.0
ddgs>=9.14.2

# ── Utilities ─────────────────────────────────────────────────
python-dotenv==1.2.1
pydantic==2.11.10
httpx==0.28.1
anyio==4.11.0
